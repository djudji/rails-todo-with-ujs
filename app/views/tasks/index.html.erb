<% if user_signed_in? %>
  <div class="container-fluid">
    
    <%= render 'tasks' %>

    <div id="modal-window" class="modal fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="false">
      <div class="row">
        <div class="col-md-6">
          <div class="modal-window modal-vertical-centered">

          </div>
        </div>
      </div>
    </div>

  </div>
<% end %>
